<!DOCTYPE html>
<html>
  <head>
    <title>Create | Let's create !</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>

    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/flatpickr/dist/flatpickr.min.css">
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>

    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Indie+Flower&family=Jersey+15&family=Permanent+Marker&family=Poppins:ital,wght@0,100;0,200;0,300;0,400;0,500;0,600;0,700;0,800;0,900;1,100;1,200;1,300;1,400;1,500;1,600;1,700;1,800;1,900&family=Roboto:ital,wght@0,100;0,300;0,400;0,500;0,700;0,900;1,100;1,300;1,400;1,500;1,700;1,900&family=Shadows+Into+Light&family=Tiny5&display=swap" rel="stylesheet">
  </head>

  <body>
    <div class="d-flex">
      <%= render "shared/navbar" %> <!-- Optionnel si tu as un navbar -->
      <%= render "shared/flashes" %> <!-- Optionnel pour les messages flash -->

      <div class="content" style="flex-grow: 1; padding: 20px;">
        <%= yield %>
      </div>
      <!-- Bottom Menu (visible partout) -->
      <%= render "shared/bottom_menu" %>

      <!-- Project Menu (masqué par défaut) -->
      <div id="project-menu-wrapper" style="display: none;">
        <%= render "shared/project_menu" %>
      </div>
    </div>
  </body>
</html>
